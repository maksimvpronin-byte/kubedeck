# KubeDeck 2.5.2 — план отображения до 2000 Pods на странице

Статус: план подготовлен, реализация не начата.

## Цель

Расширить максимальный размер страницы resource-таблицы с `1000` до `2000`, чтобы пользователь мог одновременно видеть до 2000 Pods без перехода между страницами.

Это изменение касается только отображения уже загруженных строк. Backend сейчас возвращает полный список ресурсов, поэтому новый API-запрос, параметр `limit`, cache или отдельная загрузка не нужны.

## Подтверждённый baseline

- Размеры страницы задаются общим списком `PAGE_SIZE_OPTIONS` в `useResourceTableState.ts`.
- Текущие варианты: `50`, `100`, `200`, `500`, `1000`.
- Максимум `1000`; таблица показывает остальные Pods на следующих страницах.
- `renderedRows` уже ограничивается обычным `slice(pageStart, pageStart + pageSize)`.
- Все resource-таблицы используют один selector размера страницы.

## Границы 2.5.2

В патч входят только:

- вариант `2000` в существующем selector размера страницы;
- целевой renderer contract;
- проверка большой Pods-таблицы и sticky header;
- version/release metadata 2.5.2 после реализации.

Не входят:

- изменение default page size `200`;
- автоматический выбор `2000`;
- виртуализация строк;
- новый backend endpoint или pagination API;
- отдельная настройка только для Pods;
- новые зависимости.

Поскольку список размеров страницы общий, вариант `2000` будет доступен и другим resource-таблицам. Отдельная ветка для Pods не нужна.

## Двойная проверка до изменения

- [ ] Проверка 1: подтвердить, что максимальное значение задаётся только `PAGE_SIZE_OPTIONS`, а backend не обрезает Pods до 1000.
- [ ] Проверка 2: подтвердить через renderer contract, что выбор размера страницы передаётся напрямую в `slice` и не имеет второго ограничения.
- [ ] Проверить, что выбор `1000` остаётся доступен как менее тяжёлый вариант.
- [ ] Проверить текущий sticky header из 2.5.1 на большой таблице.

## Минимальная реализация

- В `PAGE_SIZE_OPTIONS` добавить `2000` после `1000`.
- Не менять `DEFAULT_PAGE_SIZE = 200`.
- Не добавлять специальное состояние, config field или миграцию UI preferences.
- Не менять получение Pods, metrics, watch, polling и фильтрацию.

Ожидаемый production diff: одна константа в одном файле.

## Автоматические контракты

- [ ] `PAGE_SIZE_OPTIONS` точно содержит `50, 100, 200, 500, 1000, 2000`.
- [ ] Default page size остаётся `200`.
- [ ] При `pageSize = 2000` первая страница может содержать 2000 строк.
- [ ] Для 2001 строки сохраняется вторая страница с одной строкой.
- [ ] Фильтрация и сортировка выполняются до pagination.
- [ ] Sticky header contract 2.5.1 остаётся зелёным.

## Проверка производительности и интерфейса

- [ ] На 2000 Pods выбор `2000` не приводит к падению renderer или зависанию интерфейса.
- [ ] Вертикальная прокрутка остаётся рабочей, header виден сверху.
- [ ] Горизонтальная прокрутка, resize, reorder и sort столбцов работают.
- [ ] Select-all выбирает только текущую страницу из 2000 строк.
- [ ] Переключение `2000 → 1000 → 200` корректно сбрасывает номер страницы.
- [ ] Фильтр по имени/namespace корректно обновляет количество страниц.
- [ ] Watch refresh и ручной Refresh не сбрасывают выбранный размер страницы.

## Regression gate

- [ ] `npm run lint`.
- [ ] `npm run format:check`.
- [ ] `npm run test:renderer`.
- [ ] `npm run typecheck`.
- [ ] `npm run build`.
- [ ] `npm --workspace apps/desktop run test:gateway`.
- [ ] `npm run verify:release` после оформления версии 2.5.2.
- [ ] `git diff --check`.

## Критерии приёмки 2.5.2

- [ ] В selector размера страницы доступно значение `2000`.
- [ ] На одной странице отображается до 2000 Pods.
- [ ] Default остаётся `200`, существующие значения сохранены.
- [ ] Backend-загрузка и нагрузка Kubernetes API не изменены.
- [ ] Sticky header и table controls не регрессировали.
- [ ] Новые зависимости и архитектурные слои не добавлены.
- [ ] Автоматический gate и ручной smoke пройдены.
