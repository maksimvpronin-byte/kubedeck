# KubeDeck 2.0 — миграция backend на Node завершена

Дата обновления: 2026-08-10
Ветка: `main`
Текущая проверяемая версия: `2.14.0`

## Итог

Все существующие backend-контракты перенесены из Python/FastAPI в Node.js внутри Electron main process.

- Node routes: **57**.
- Python routes: **0**.
- Runtime mode: **node-only**.
- Legacy HTTP/WebSocket proxy: удалён.
- Python/FastAPI child process: удалён.
- PyInstaller packaging: удалён.
- Python backend payload в portable: запрещён проверкой сборщика.
- Встроенный `kubectl.exe`: запрещён проверкой сборщика.

## Выполненные этапы

- Alpha 1–2: Node Gateway, config, audit и cluster management.
- Alpha 3–4: Node kubectl runtime, resource details, logs, YAML, Secrets, actions и Pod Exec.
- Alpha 5: resource lists и Resource Snapshot Cache.
- Alpha 6: Resource Watch и WebSocket Event Hub.
- Alpha 7: Port Forward Manager.
- Alpha 8: Pod Terminal и ConPTY/pipe fallback.
- Alpha 9: Node SSH, private key, agent и jump host.
- Alpha 10: Problems Engine.
- Alpha 11: Global Search.
- Alpha 12: Related Resources.
- Alpha 13: LLM status/test/preview/analyze.
- Alpha 14: удаление Python runtime, legacy proxy и PyInstaller.
- Alpha 15: стабилизация Node-only build/test/documentation pipeline.

## Alpha 15 — принято

Проверено:

- `npm.cmd run verify:node-only` проходит;
- TypeScript typecheck и desktop build проходят;
- Gateway suite запускается с `--test-concurrency=1`;
- process-heavy Watch и Port Forward тесты проходят без cancelled;
- Windows portable собирается;
- приложение запускается без `python.exe`/`pythonw.exe`;
- `/migration/status`: Node 52, Python 0, mode `node-only`;
- portable не содержит Python backend payload и встроенный `kubectl.exe`.

## 2.0.6 — stable release baseline

`2.0.6` фиксирует проверенный Node-only baseline и включает пользовательские UX-исправления, накопленные после первичной стабилизации.

Исторические release notes и checklist 2.0.6 удалены после переноса итогов в `CHANGELOG.md`. Актуальные проверки находятся в `docs/releases/REGRESSION_CHECKLIST_2.9.3.md` и `docs/release-checklist.md`.

## 2.10.1 — шифрование LLM API key

`2.10.1` закрывает долг, зафиксированный в плане `2.10.0`: LLM API key больше
не хранится в `config.json` открытым текстом, а шифруется через Electron
`safeStorage` в `secrets/llm-api-key.bin` с one-shot миграцией существующих
ключей. Маршрутов не прибавилось — `PUT /settings` и `POST /llm/test`
расширили контракт существующих routes. Node-only Gateway остаётся на 54
маршрутах.

## 2.10.2 — устранение дублирования backend/renderer

`2.10.2` — внутренний cleanup-патч по итогам повторного код-ревью: один
`decodePathPart`, один `writeRouteError`-диспетчер и один `RouteInfoError`
вместо 14+12+4 копий в routes/WebSocket-модулях; CPU/memory quantity парсится
один раз в `resources/quantity.ts` (раньше `normalizers.ts` не поддерживал
`Pi`/`Ei` и не делал truncation, которые уже были в `metrics.ts` — теперь
поведение одинаковое). На renderer — `PodDrawer.tsx` лишился logs- и
YAML-кластеров (вынесены в hooks), `NodeSshTab.tsx`/`TerminalTab.tsx` делят
общий `utils/xtermSession.ts`, удалён мёртвый `EventsTab.tsx`. Маршрутов не
прибавилось, Node-only Gateway остаётся на 54 маршрутах.

## 2.14.0 — проход по производительности

`2.14.0` — минорная. Таблица ресурсов перерисовывала все строки раз в секунду
ради колонки Age: часы переехали в саму ячейку, один таймер на всех читателей,
сравнивается готовый текст, поэтому под старше суток не перерисовывается вовсе.
Ящик пода тикал на всех вкладках, хотя возраст показывает только Summary.
Сортировка перешла с `localeCompare` с объектом опций на кэшированный
`Intl.Collator` — 217 мс против 11 мс на 5000 строк при том же порядке.
Стабилизированы две идентичности, обнулявшие мемоизацию фильтра и сортировки.
Кэш снапшотов в main-процессе чистит просроченные записи при записи, а не
только при чтении. Единственное изменение поведения — коалесинг watch-событий:
к задержке 350 мс добавлены минимальный интервал 1 с и максимальное ожидание
3 с, потому что поток событий чаще 350 мс не обновлял таблицу никогда, а чуть
реже — давал полный `kubectl get -A -o json` на каждое событие. Маршрутов
по-прежнему **Node 58 / Python 0**.
## 2.13.4 — Help и About снова описывают приложение

`2.13.4` — патч. Быстрый старт отправлял выбирать кластер в верхней панели,
хотя кластеры давно на левой рельсе. Help дополнен подключением кластеров и
значками состояния, вкладками LLM и Secret, историей потребления, разделами
Overview, Port-forwards и Audit, а также Ctrl+K. В About добавлена карточка
лицензий: Apache-2.0, копирайт дословно из NOTICE и указание на сторонние
компоненты. Маршрутов по-прежнему **Node 58 / Python 0**.
## 2.13.3 — скрытие промпта больше не ждёт модель

`2.13.3` — патч. Кнопка Hide prompt блокировалась общим флагом занятости вместе
с анализом, хотя закрытие промпта ничего не запрашивает: обработчик сбрасывает
флаг и выходит. Ждать есть чего только при открытии. Теперь кнопка зависит
лишь от собственной загрузки. Маршрутов по-прежнему **Node 58 / Python 0**.
## 2.13.2 — видно, но недоступно

`2.13.2` — патч. В светлой теме пропадали цифры в терминале: в xterm
передавались только восемь из шестнадцати ANSI-цветов, и весь набор bright
оставался дефолтным, то есть подобранным под тёмный фон. В панели проблем путь
ресурса переносился по одной букве, потому что адаптив смотрел на окно, а не на
саму панель. Поиск в YAML считал совпадения, но не прокручивал к ним — скролл
писался в textarea, которая после перехода на фолдинг перестала прокручиваться.
Маршрутов по-прежнему **Node 58 / Python 0**.
## 2.13.1 — сохранение настроек больше не гасит рельсу кластеров

`2.13.1` — патч. Кнопка Save в настройках красила все значки кластеров в цвет
отключённого, хотя ничего не отключалось. Полный `AppConfig` возвращают два
маршрута, `GET /config` и `PUT /settings`, и состояние подключений было
добавлено только в первый. Теперь оба собираются одним построителем.
Маршрутов по-прежнему **Node 58 / Python 0**.
## 2.13.0 — подключение кластера стало осознанным действием

`2.13.0` — минорный релиз. Кластер больше не остаётся подключённым навсегда:
каждый однажды открытый оставлял за собой сэмплер метрик и по процессу
`kubectl get --watch` на каждый просмотренный вид ресурсов, и ничего из этого
не останавливалось при переходе к другому кластеру. На рельсе появились
подключение и отключение по правой кнопке и индикация состояния рамкой.
Отключение освобождает watch, сэмплер, проброс портов, терминалы, SSH и кэши —
и держит их освобождёнными.

Сбор метрик переведён с таблицы `kubectl top` на Metrics API, интервал — 15
секунд с отсевом повторного скрейпа по `timestamp`. Маршрутов стало
**Node 58 / Python 0**.
## 2.12.1 — колонка Usage снова показывает потребление

`2.12.1` — патч по колонке Usage, которая застывала на том, что успела
поймать последняя загрузка таблицы. Три причины. Нулевое показание считалось
отсутствием данных: наличие метрики определялось сравнением суммы с нулём, и у
простаивающего пода с `0m` CPU истории не было вовсе. Теперь CPU и память
ведут собственные счётчики замеров — это заодно исправило их средние, потому
что memory metrics-server отдаёт с первого скрейпа, а CPU только со второго, и
деление обеих метрик на общий счётчик занижало ту, что приехала позже. Окно
считалось по границам пятиминутных бакетов и завышалось — теперь по реальным
меткам времени. Колонка не обновлялась вообще: таблица живёт на watch-событиях,
а устоявшийся под их не порождает. Добавлен `GET
/clusters/{cluster_id}/pod-usage`, который отдаёт текущее потребление подов из
уже записанных сэмплов и не вызывает kubectl совсем, поэтому таблица обновляет
числа раз в 30 секунд, не платя за `kubectl get pods`. Node-only Gateway — 57
маршрутов.

## 2.12.0 — история потребления подов и русские ответы LLM

`2.12.0` — KubeDeck сам записывает потребление подов и показывает его в
сводке. Prometheus за этим нет: сэмплы снимает само приложение, поэтому
история покрывает только время его работы, и рядом с каждой цифрой указано,
какую долю 24-часового окна она покрывает. Сбор не стоит ничего
дополнительного: вызов `kubectl top pods`, который и так делает таблица,
записывается по пути, а фоновый `kubectl top pods -A` раз в минуту работает
для открытых кластеров. Сэмплы складываются в пятиминутные бакеты (среднее,
пик, число замеров), хранятся сутки и пишутся в `%APPDATA%/KubeDeck/metrics`,
так что перезапуск не обнуляет окно; на кластер не больше 2000 серий, удаление
кластера уносит его историю. p50/p95 считаются по средним внутри бакетов —
это устойчивая нагрузка, под которую сайзится request, — а max это пик,
который должен переживать limit. Ключ истории переживает редеплой: хэш
`pod-template-hash` отрезается, и остаётся Deployment, а не эфемерный
ReplicaSet. Реплики пулятся, а не суммируются. В сводке пода появился бар по
CPU и RAM с линиями request/limit, те же числа уходят в контекст LLM отдельной
секцией с указанием покрытия. Отдельно исправлено: анализ LLM отвечал
по-английски, потому что рендерер слал `system` из настроек, а промпт знал
только `ru`/`en`; теперь язык не параметр запроса, ответ всегда русский, а
Kubernetes-термины не переводятся. Маршрутов не прибавилось, Node-only Gateway
остаётся на 56 маршрутах.

## 2.11.2 — Traefik и Gateway API в Related, эндпоинты сервиса, редактируемый YAML

`2.11.2` — патч по вкладкам drawer. Related знал только встроенный `Ingress`
и искал его лишь от Service, поэтому на кластере с Traefik или Gateway API
маршрутизация не показывалась вообще, а под не показывал её даже с обычным
`Ingress`. Добавлены связи в обе стороны для `IngressRoute`,
`IngressRouteTCP`, `IngressRouteUDP`, `Middleware`, `HTTPRoute`, `Gateway`
и `GatewayClass`, включая транзитивный переход от пода через его сервисы.
Роутовые CRD запрашиваются только если кластер их отдаёт: маршрут читает тот
же кэш `kubectl api-resources`, что и Global Search, поэтому кластер без
Traefik не платит за лишний вызов и не получает ошибку в диагностике; оттуда
же берётся признак namespaced, а сам запрос идёт параллельно загрузке
объекта. Сводка Service показывает эндпоинты из `EndpointSlice` — готовые из
общего числа и список с адресом, портами и подом; это добавило операцию
`endpoints` к маршруту resource details. Drawer запоминает вкладку
отдельно для каждого типа ресурса, а вкладка YAML редактируется сразу, без
кнопки Edit, сохраняя сворачивание секций. Маршрутов не прибавилось,
Node-only Gateway остаётся на 56 маршрутах.

## 2.11.1 — скорость графиков нод, выбор namespace и CPU без лимитов

`2.11.1` — патч по трём пользовательским замечаниям. Маршрут списка ресурсов
ждал `kubectl get` и только потом запускал `kubectl top`, поэтому Nodes, Pods
и Namespaces платили за два последовательных вызова kubectl — теперь команда
метрик и запрос resource quota выполняются параллельно списку, включая
cluster overview. Disk-метрики нод (по одному процессу
`kubectl get --raw .../stats/summary` на ноду) грузятся в шесть потоков
вместо двух, а обновления строк склеиваются в одно обновление таблицы.
Исправлена потеря выбранного namespace: фоновый refresh перезаписывал
запомненный выбор кластера безусловно — в том числе пока открыт
cluster-scoped ресурс и для уже покинутого кластера; восстановление падало на
`all`, если запомненного выбора нет; переход из Global Search, Events и
Related всегда сужал селектор до namespace объекта и запоминал это. Колонка
Usage у подов теперь падает с лимита на request и далее на сырое значение,
поэтому CPU виден и без лимитов. Маршрутов не прибавилось, Node-only Gateway
остаётся на 56 маршрутах.

## 2.11.0 — namespace scope таблицы, рельс кластеров и иконка Windows

`2.11.0` — таблица ресурсов больше не показывает строки предыдущего
namespace: строки принадлежат scope (cluster, resource, namespace selection)
и очищаются до запроса при его смене, а silent refresh от watch не прерывает
выполняющуюся загрузку того же scope. Переключение кластеров переехало из
выпадающего списка в топбаре в вертикальный рельс иконок слева от навигации.
На Windows запущенное приложение получило иконку KubeDeck вместо дефолтной
Electron (icon у окна, ассеты в payload, rcedit и AppUserModelID). Добавлено
редактирование kubeconfig кластера из Settings: два новых маршрута
(`GET`/`PUT /clusters/{cluster_id}/kubeconfig`), Node-only Gateway — 56
маршрутов.

## 2.10.3 — устранение утечки памяти и проблем производительности

`2.10.3` — патч по итогам performance-аудита backend/renderer: `WatchManager`
никогда не удалял завершённые watch-сессии из `this.sessions` — теперь
явная остановка удаляет сессию сразу, а сессии, упавшие без явного `stop()`,
удаляются TTL-sweep'ом (5 минут) при вызове `status()`. Также: убран лишний
`structuredClone` в resource cache; `routes/search.ts` переиспользует общий
кэш `kubectl api-resources` вместо дублирующего вызова на каждый ввод в
поиске; кэш disk-метрик ноды (30s TTL) убирает N+1 kubectl-вызовов на каждый
overview-poll; построение `Set` в `matchingDeploymentPods()` вынесено из
цикла по подам; O(n²)-фильтр выбора строк в таблице ресурсов и четыре
немемоизированных renderer-вычисления (колонки таблицы, YAML search,
Manifest Compare diff, Logs фильтрация) исправлены через `useMemo`.
Маршрутов не прибавилось, Node-only Gateway остаётся на 54 маршрутах.

## Следующий этап

`2.9.3` сохраняет единый Terminal Workspace, исправляет packaged UI и Windows packaging, а также добавляет тему Steel Graphite. Node-only Gateway содержит 52 маршрута.

После принятия 2.0.6:

1. commit и push stable baseline;
2. закрыть ручной regression checklist;
3. исправлять найденные дефекты отдельными небольшими patch-релизами;
4. готовить следующий стабильный patch/minor-релиз по итогам проверки.

## Правила работы

- Стабильные изменения выпускаются из ветки `main`.
- Один ZIP-патч — один логический этап.
- Перед ZIP-патчем согласуется план.
- Не выполнять `npm ci` без необходимости.
- Не добавлять `kubectl.exe` в portable.
- Не использовать `git diff` в инструкциях.
